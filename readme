(See history.md for initial GitHub project state and "release notes".)
 
The Circulospherical Infinity Engine (CIE) was initially created ~May 24, 2026 in the web app Google Gemini 3  After some technical issues in trying to save the code, it was lost, and I recreated CIE the next day and, after a couple days, started hitting the "prompt/query token limit" (whatever it's called)...on a 45K file. So, I tried using Anthropic Claude and, while it handled the 45K file fine, its free tier is more limited in what it can do (maybe 2 full builds) every 5 hours.

Then I discovered Google AI Studio, which basically uses the same Gemini as the web app (i.e. Google Search) but has a much larger prompt/query window and I have yet to reach the free-tier limits after having done about 15 "milestones" (what it called "versions" until I gave it new direction (see history.md for more details).

Speaking (typing) of versions, version numbering since using the Gemini web app up to what it dubbed "version 5" used an "integral" rapid version numbering system instead of the more rational decimal technique ("semantic versioning"). I have decided to collapse the 5.x version into a more manageable 1.x version to more easily facilitate many changes ("builds") without bloating the overall version number (hi, Google Chrome v.freaking 148! 🙄🤦‍♂️).

Anyway, that's where the project is now but I'm excited to continue "vibe coding" it! I never enjoyed programming beyond scripting--even Javascript is too much but other, less complex scripting languages like HTML, CSS, RenderWare Script (RWX), Second Life Linden Scripting Language (LSL), and even Active Worlds action commands I've used (with LSL probably being the most difficult, but easier, than Javascript).

Anyanyway, enjoy!

Late-August 2026 update: Google has drastically reduced Gemini 2.7 Flash free-tier quota limits, so I will be moving to another AI model—most likely locally-run. It was a good run but Gemini (mostly 3.6 Flash) has been claiming to fix/add things that it did not, and I have to waste more prompts re-asking/-wording queries and hope they finally get done.
